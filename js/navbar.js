// This file updates the navbar based on authentication state
// It's already being used in auth.js when the page loads

document.addEventListener('DOMContentLoaded', () => {
    // The auth.js file will handle navbar updates
    // This ensures all pages have consistent navbar behavior
});
